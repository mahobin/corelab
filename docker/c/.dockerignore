# docker/c/.dockerignore
# Exclude everything except the C environment context files.
# The build context is docker/c/ so paths are relative to that directory.

**/__pycache__/
**/*.pyc
**/*.pyo
**/.DS_Store
**/Thumbs.db
**/*.log
**/tmp/
**/temp/
